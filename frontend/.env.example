# The backend API the Next.js app proxies /api/* to (see next.config.js).
# Local dev default is http://localhost:4000.
#
# For the deployed backend, set:
# BACKEND_URL=https://vibrant-wasp-attendance-backend.cloud.nexlayer.ai
BACKEND_URL=http://localhost:4000
